# Supabase Project URL
# Replace "YOUR_SUPABASE_URL" with the actual URL from your Supabase project settings (API > URL).
NEXT_PUBLIC_SUPABASE_URL="YOUR_SUPABASE_URL"

# Supabase Anon Key (Public Key)
# Replace "YOUR_SUPABASE_ANON_KEY" with the actual anon (public) key from your Supabase project settings (API > Project API keys).
NEXT_PUBLIC_SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_KEY"

# IMPORTANT:
# 1. Create a new file named ".env" in the root of your project (or ".env.local" for local development overrides).
# 2. Copy the contents of this example file into your new .env (or .env.local) file.
# 3. Replace the placeholder values "YOUR_SUPABASE_URL" and "YOUR_SUPABASE_ANON_KEY" with your actual Supabase credentials.
# 4. After creating/updating your .env or .env.local file, you MUST RESTART your Next.js development server
#    for these changes to take effect. If you don't restart, the old (or missing) values will still be used.

# Example for Genkit with Google AI Studio (if needed)
# GOOGLE_API_KEY="YOUR_GOOGLE_API_KEY_IF_NEEDED"
